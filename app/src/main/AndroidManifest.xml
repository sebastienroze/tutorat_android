<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ifa.devlog.tutorat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />


    <uses-feature android:name="android.hardware.camera"
        android:required="true" />
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlocNote"
        android:usesCleartextTraffic="true">
        <activity android:name="ifa.devlog.tutorat.view.VoirContenuActivity" />
        <activity android:name="ifa.devlog.tutorat.view.ListeQuestionActivity" />
        <activity android:name="ifa.devlog.tutorat.view.EditionActivity" />
        <activity android:name="ifa.devlog.tutorat.view.EditionReponseActivity" />
        <activity android:name="ifa.devlog.tutorat.view.VoirReponseActivity" />
        <activity android:name="ifa.devlog.tutorat.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

</manifest>